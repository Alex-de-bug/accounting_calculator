import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    // mode: 'dark',
    primary: {
      main: '#eff0e9',
    },
    secondary: {
      main: '#000000',
    },
    nav: {
      main: '#595959',
    },
    black: {
      main: '#000000',
    }
  },
});

export default theme;